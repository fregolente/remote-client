/* eslint-disable no-unused-vars */
const styles = theme => ({
  primary: {
    color: '#FFF',
    textDecorationLine: 'underline',
  },
  input: {
    padding: '14px',
  },
});

export default styles;
